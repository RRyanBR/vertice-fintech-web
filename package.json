{
  "name": "vertice-fintech-web",
  "version": "1.0.0",
  "description": "Vértice – Fintech de controle financeiro pessoal (FIAP – Fase 4)",
  "scripts": {
    "build": "tailwindcss -i ./input.css -o ./style.css --minify",
    "dev": "tailwindcss -i ./input.css -o ./style.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.19"
  }
}
